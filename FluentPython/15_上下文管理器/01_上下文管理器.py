# !/usr/bin/env python# -*- coding:utf-8 -*-# name: 01_上下文管理器 # author: li zhang# date: 2019/11/26for i in range(10):    print(i)else:    print('i am done')# while else# 除了if语句之外，其他语句选择使用else关键字是个错误# 在循环中，理解为 运行这个循环，然后做那件事# my_list = []# for item in my_list:#     if item.flavor == 'banana':#         break# else:#     raise ValueError('No banana flavor found')# 上下文管理器和with块# 上下文管理器对象存在的目的是管理with语句，就像迭代器的存在就是为了管理for语句# with语句的目的是简化import syssys.stdin# 可以自己实现一个上下文管理器dir = {1:False}print(all(dir.values()))l = {1: 1, 3: -1, 5: 1, 7: 0}for i in l.values():    print(i != 1)