# !/usr/bin/env python# -*- coding:utf-8 -*-# name: 02_格式化显示 # author: li zhang# date: 2019/11/7brl = 1/2.43print(brl)# 使用 format 函数print(format(brl, '0.4f'))# 使用 str.format() 函数s = '1 BRL = {rate:0.2f} USD'.format(rate=brl)print(s)# 名词：格式化规范微语言# python 的私有属性是一种保护属性，不是一种安全机制# 一个下划线开头只是程序员们默认的共识，实际上python的保护的机制规范写法为 两个下划线开头# 但是在模块中，在顶层模块中，下划线的名称不会被导入# 总结：python的保护机制只有一种，python程序员默认的私有属性为下划线，并不是python# python的两种保护机制，两个特点